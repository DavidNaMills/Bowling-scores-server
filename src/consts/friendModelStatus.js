const OK = 'OK';
const PENDING = 'PENDING';
const BLOCKED = 'BLOCKED';

const FRIEND_STATUS_ARRAY = [OK, PENDING, BLOCKED];

module.exports = {
    OK,
    PENDING,
    BLOCKED,
    FRIEND_STATUS_ARRAY
}